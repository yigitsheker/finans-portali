apiVersion: v1
kind: Service
metadata:
  name: playwright-service
  labels:
    app.kubernetes.io/name: playwright-service
    app.kubernetes.io/component: scraper
spec:
  selector:
    app.kubernetes.io/name: playwright-service
  ports:
    - name: http
      port: 3000
      targetPort: http
